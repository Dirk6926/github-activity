# Never commit real secrets!
DATABASE_URL=postgres://user:pass@localhost:5432/app_db
JWT_SECRET=super-secret-key
API_KEY=abc123
```

> Apps load this with tools like **dotenv** (Node), **python-dotenv** (Python), or framework config.